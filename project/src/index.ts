console.log("ts is not here");

// You can run typescript compiler in watch mode
// so that any changes that you save get compiled in
// javasrit in the dist/ directory. This does not happen
// when you manually compile as the destination directory
// remains the same. 